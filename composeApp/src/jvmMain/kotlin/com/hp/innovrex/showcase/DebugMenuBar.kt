package com.hp.innovrex.showcase
}
    }
        }
            )
                )
                    ctrl = true
                    key = androidx.compose.ui.input.key.Key.K,
                shortcut = androidx.compose.ui.input.key.KeyShortcut(
                onClick = onOpenShowcase,
                "Design System Showcase",
            Item(
        Menu("Debug", mnemonic = 'D') {
    MenuBar {

    if (!IS_DEBUG) return
) {
    onOpenShowcase: () -> Unit
fun DebugMenuBar(
@Composable
 */
 * Adds a "Debug" menu to the menu bar with showcase option.
 * Desktop-specific showcase launcher.
/**

import androidx.compose.ui.window.MenuBar
import androidx.compose.runtime.*
import androidx.compose.material3.*


